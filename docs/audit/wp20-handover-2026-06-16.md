# WP20 handover — 2026-06-16

This note is the handover source for the Codex takeover after the Claude
`dataviz` loop stopped on the weekly rate limit. It reconciles the current Git
state, the local track ledger, and the final Claude prompt.

## Current source of truth

- Git `main` is aligned with `origin/main` at `cff276725d0c7639936b2fcdfcfa5eaa32538491`.
- The operational state is in `BACKLOG.md`, plus `docs/chart-ux-qa.md` and
  `docs/color-scales.md`.
- `.track/` is a local, untracked ledger. It is technically hash-consistent but
  functionally stale: it has 62 events and stops at 2026-06-12, before the
  `v0.4.45` to `v0.4.48` work and before the DS ACK on 2026-06-16.
- Do not treat `.track/` open items as the real backlog. It still lists several
  delivered items as `to-do` or `in-progress` (PDF, Intl formatting,
  PalettePicker/color scales, Rose/Chord/PackedBubble, sidebar mobile).

## Claude handover summary

Final Claude loop prompt, 2026-06-16 03:17 local session:

- Solo dataviz work is considered exhausted.
- Three remaining WP20 items are DS-blocked and formally tracked on the design
  system side:
  - `DashboardGrid` for dashboard edit mode (`columns`, `tiles{id,x,y,w,h}`,
    `editable`, `onLayoutChange`), mapping to existing `dataviz-core/layout.ts`.
  - Heatmap sequential scale via agreed API
    `scale?: 'categorical' | 'sequential'`, applicable to HeatmapChart and
    continuous heatmap-like charts.
  - Tooltip nearest-X/crosshair/hide-on-leave, specified in
    `docs/chart-ux-qa.md`.
- DS state at last Claude check: svelte `0.34.49`, react/vue `0.36.45`, active
  on NavSystem wave 2. Do not send more DS requests before the DS pings or a
  matching FR lands.

## What not to do

- Do not implement consumer-side workarounds for heatmap color. The DS currently
  maps continuous values to categorical `category1..8` CSS classes and exposes no
  hex/color escape hatch.
- Do not bump DS dependencies for unrelated DS releases such as NavSystem,
  AppShell, themes, or chrome-only work.
- Do not wire a feature if only one or two frameworks are published. Wait for
  svelte, react, and vue parity.
- Do not publish another dataviz release without reconciling lockstep package
  versions.

## Release drift reconciled

After tag `v0.4.48`, `main` contains six commits. Two of them added/fixed
React-only time-series/dashboard chart work:

- `packages/dataviz-core` is `0.4.49`.
- `packages/dataviz-react` is `0.4.50`.
- `packages/dataviz-svelte` and `packages/dataviz-vue` remain `0.4.48`.

Codex takeover action on 2026-06-16:

- Aligned `dataviz-core`, `dataviz-react`, `dataviz-svelte`, and `dataviz-vue`
  package versions to `0.4.50`.
- Aligned all internal `@sentropic/dataviz-core` dependencies to `0.4.50`.
- Regenerated `package-lock.json`, which removed the stale nested registry
  installs of `@sentropic/dataviz-core@0.4.48` under svelte/vue.

Do not publish until the normal gate has passed. The repository convention
remains lockstep OIDC release for the four packages.

## Next safe action

After the lockstep cleanup, poll DS and do not code WP20 FRs unless a DS FR is
actually delivered:

1. Check latest DS package versions for svelte/react/vue.
2. Check recent `rhanka/sent-tech-design-system` commits for one of the three
   explicit FR names or APIs above.
3. If and only if a FR is delivered in 3-framework parity, bump the three DS
   dependencies, inspect the nested installed `.d.ts` contracts, wire the
   matching dataviz feature, run the full gate, and release lockstep.
4. If no matching FR is delivered, keep the repo idle except for documentation or
   explicit user-requested QA/reporting.

Minimal local verification:

```bash
git status --short --branch
git describe --tags --always --dirty
npm run verify
```

Use `npm run verify` only when preparing a functional change or release; the
handover itself is documentation-only.
